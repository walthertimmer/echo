[project]
name = "echo"
version = "1"
description = "Transcribe"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "transformers",
    "torch",
    "soundfile",
    "jinja2",
    "python-multipart"
]
